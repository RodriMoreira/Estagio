//Declaração de variáveis.
const INDICE = 13;
let SOMA = 0;
let K = 0;

//Laço de repetição que calcula o resultado das operações.
while(K < INDICE) {
  K = K + 1
  SOMA = SOMA + K
}

//Impressão da variável SOMA.
console.log(SOMA);